export function ProgressBar() {
  return (
    <div aria-label="Project progress placeholder" className="progress-shell">
      {/* TODO Stage 4: Accept progressPercent and render accessible progress semantics. */}
    </div>
  );
}
