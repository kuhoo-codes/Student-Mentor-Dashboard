export function HealthBadge() {
  return (
    <span className="placeholder-badge">
      {/* TODO Stage 4: Map project health status to clear badge labels and colors. */}
      Health status
    </span>
  );
}
