export function ProjectList() {
  return (
    <section className="placeholder-card" aria-labelledby="project-list-title">
      <h2 id="project-list-title">Project List</h2>
      {/* TODO Stage 2: Render mock student projects from src/data. */}
      {/* TODO Stage 3: Make each project selectable and expose the selected project to the detail card. */}
      <p>Project list will appear here.</p>
    </section>
  );
}
