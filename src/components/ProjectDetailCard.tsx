export function ProjectDetailCard() {
  return (
    <section className="placeholder-card" aria-labelledby="project-detail-title">
      <h2 id="project-detail-title">Project Detail Card</h2>
      {/* TODO Stage 4: Render health status, progress, current milestone, and next actions. */}
      <p>Selected project details will appear here.</p>
    </section>
  );
}
